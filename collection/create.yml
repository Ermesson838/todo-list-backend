info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/items
  body:
    type: json
    data: |-
      {
        "name": "Rian",
        "description": "16",
        "id": 16
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
